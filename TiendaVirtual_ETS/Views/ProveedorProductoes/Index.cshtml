@model IEnumerable<TiendaVirtual_ETS.Models.ProveedorProducto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProveedorProductoID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proveedor.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ProveedorProductoID)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Producto.Descripcion)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Proveedor.Nombre)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.ProveedorProductoID }) |
        @Html.ActionLink("Details", "Details", new { id = item.ProveedorProductoID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ProveedorProductoID })
    </td>
</tr>
}

</table>
